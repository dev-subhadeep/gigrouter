meta {
  name: Login User
  type: http
  seq: 2
}

post {
  url: {{process.env.BASE_URL}}:{{process.env.PORT}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "subhadeep@gmail.com",
    "password": "abc123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
